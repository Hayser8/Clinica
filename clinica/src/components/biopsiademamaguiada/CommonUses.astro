---
import { Target, Search, AlertCircle, Phone } from "@lucide/astro";

const uses = [
  {
    icon: Target,
    title: "Una masa sólida sospechosa",
    description:
      "Nódulos o masas que requieren análisis detallado para determinar su naturaleza",
  },
  {
    icon: Search,
    title: "Una distorsión en la estructura del tejido mamario",
    description:
      "Cambios en la arquitectura normal del tejido que necesitan evaluación",
  },
  {
    icon: AlertCircle,
    title: "Un área de cambio anormal en el tejido",
    description:
      "Alteraciones detectadas en estudios previos que requieren confirmación",
  },
];
---

<section class="py-16 md:py-20 bg-white">
  <div class="max-w-7xl mx-auto p-8">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-8 text-center">
        ¿Cuáles son algunos de los usos comunes de este procedimiento?
      </h2>
      <p
        class="text-lg text-gray-700 mb-12 text-center leading-relaxed max-w-4xl mx-auto"
      >
        Una biopsia de mama guiada por ultrasonido se puede llevar a cabo cuando
        el estudio de ultrasonido, resonancia o mamografía de la mama muestra
        una anormalidad tal como:
      </p>

      <div class="grid md:grid-cols-3 gap-8 mb-12">
        {
          uses.map((use, index) => (
            <div
              key={index}
              class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow"
            >
              <div class="bg-pink-100 p-3 rounded-lg w-fit mb-4">
                <use.icon class="w-6 h-6 text-pink-600" />
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-3">
                {use.title}
              </h3>
              <p class="text-gray-700 leading-relaxed">{use.description}</p>
            </div>
          ))
        }
      </div>

      <div class="bg-pink-50 border-l-4 border-pink-500 p-6 rounded-r-lg">
        <p class="text-lg text-pink-800 leading-relaxed">
          <strong>Nota importante:</strong> Hay momentos en que tu médico podría
          decidir que el diagnóstico por imágenes de ultrasonido para la biopsia
          sea adecuado incluso para una masa que se puede palpar.
        </p>
      </div>

      <!-- CTA -->
      <div class="text-center mt-10">
        <a
          href="/hacer-cita"
          class="inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold bg-gradient-to-r from-pink-600 to-pink-700 text-white hover:from-pink-700 hover:to-pink-800 transition-colors w-full sm:w-auto"
          aria-label="Agenda tu Cita"
        >
          <Phone class="w-5 h-5 mr-2" />
          Agenda tu Cita
        </a>
      </div>
  </div>
</section>
