---
import { Zap, Shield, Clock, CheckCircle } from "@lucide/astro";
const features = [
  {
    icon: Shield,
    title: "100% Seguro",
    description: "Sin radiación ni efectos secundarios",
  },
  {
    icon: Zap,
    title: "Sin dolor",
    description: "Procedimiento completamente indoloro",
  },
  {
    icon: Clock,
    title: "Resultados inmediatos",
    description: "Imágenes en tiempo real durante el estudio",
  },
];

const benefits = [
  "Imágenes en tiempo real",
  "Sin radiación",
  "Evaluación completa",
];
---

<section class="py-16 md:py-20 bg-white">
  <div class="max-w-7xl mx-auto p-8">
    <div class="space-y-12">
      <div class="text-center space-y-6">
        <div
          class="inline-flex items-center bg-pink-100 text-pink-600 px-4 py-2 rounded-full text-sm font-medium"
        >
          Tecnología médica avanzada
        </div>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900">
          ¿Qué es el ultrasonido testicular?
        </h2>
      </div>

      <div class="bg-gray-50 rounded-3xl p-8 md:p-12">
        <div class="flex flex-col md:flex-row items-start gap-8">
          <div class="flex-shrink-0">
            <div
              class="w-16 h-16 bg-pink-600 rounded-2xl flex items-center justify-center"
            >
              <Zap class="w-8 h-8 text-white" />
            </div>
          </div>
          <div class="space-y-6">
            <p class="text-lg md:text-xl text-gray-700 leading-relaxed">
              Es un estudio de exploración por imágenes que utiliza ondas
              sonoras sin radiación para ver tus testículos y las vías urinarias
              en tiempo real. Asimismo, permite observar la forma, el tamaño y
              la textura del tejido, además, de estructuras vecinas como la
              vejiga.
            </p>
            <div class="flex flex-wrap gap-6">
              {
                benefits.map((benefit, index) => (
                  <div key={index} class="flex items-center gap-2">
                    <CheckCircle class="w-5 h-5 text-pink-600" />
                    <span class="text-pink-600 font-medium">{benefit}</span>
                  </div>
                ))
              }
            </div>
          </div>
        </div>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        {
          features.map((feature, index) => (
            <div
              key={index}
              class="bg-white rounded-2xl p-8 text-center space-y-4 shadow-sm hover:shadow-md transition-shadow"
            >
              <div class="w-16 h-16 bg-pink-600 rounded-2xl flex items-center justify-center mx-auto">
                <feature.icon class="w-8 h-8 text-white" />
              </div>
              <h3 class="text-xl font-bold text-gray-900">{feature.title}</h3>
              <p class="text-gray-600 leading-relaxed">{feature.description}</p>
              <div class="w-12 h-1 bg-pink-600 rounded-full mx-auto" />
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
