---
import { Zap, CheckCircle, Eye, Shield, Clock } from "@lucide/astro"
const benefits = [
  { icon: CheckCircle, text: "100% Seguro", color: "text-emerald-600" },
  { icon: Shield, text: "Sin dolor", color: "text-blue-600" },
  { icon: Clock, text: "Resultados inmediatos", color: "text-purple-600" },
]
---
<section class="py-16 md:py-24 bg-gradient-to-br from-gray-50 to-pink-50">
  <div class="max-w-7xl mx-auto p-8">
    <div class="text-center mb-12">
      <div
        class="inline-flex items-center gap-2 bg-pink-100 text-pink-600 rounded-full px-4 py-2 text-sm font-medium mb-6"
      >
        <Zap class="w-4 h-4" />
        Tecnología médica avanzada
      </div>
      <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 mb-6">
        ¿Qué es la biopsia de tiroides guiada por ultrasonido?
      </h2>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div
        class="lg:col-span-2 w-full bg-white rounded-3xl p-8 md:p-12 shadow-xl border border-gray-100"
      >
        {/* Icono arriba, texto abajo en mobile; lado a lado en md+ */}
        <div class="flex flex-col md:flex-row items-start gap-4 md:gap-6 mb-8">
          <div
            class="w-12 h-12 md:w-16 md:h-16 bg-gradient-to-br from-pink-500 to-pink-600 rounded-xl md:rounded-2xl flex items-center justify-center flex-shrink-0 shadow-lg mb-3 md:mb-0"
          >
            <Eye class="w-7 h-7 md:w-8 md:h-8 text-white" />
          </div>
          <div class="space-y-4 flex-1">
            <p class="text-lg md:text-xl text-gray-700 leading-relaxed">
              Es un procedimiento ambulatorio que utiliza ultrasonido en tiempo
              real para dirigir una aguja delgada hacia el nódulo o área
              sospechosa de la glándula tiroides.
            </p>
            <p class="text-lg text-gray-600">
              Gracias a la imagen ecográfica, obtenemos una muestra de células
              con gran precisión y mínima molestia.
            </p>
          </div>
        </div>

        <div
          class="bg-gradient-to-r from-pink-50 to-purple-50 rounded-2xl p-6 mb-8"
        >
          <p class="text-lg font-medium text-gray-800 mb-2">Ventaja clave:</p>
          <p class="text-gray-700">
            Es una herramienta clave para diferenciar entre lesiones benignas y
            malignas sin necesidad de cirugía invasiva.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          {
            benefits.map((benefit, index) => (
              <div
                key={index}
                class="flex items-center gap-3 bg-gray-50 rounded-xl p-4"
              >
                <benefit.icon class={`w-5 h-5 ${benefit.color}`} />
                <span class="font-medium text-gray-800">{benefit.text}</span>
              </div>
            ))
          }
        </div>

        {/* Botón: centrado en desktop, full en mobile */}
        <div class="flex justify-start md:justify-center">
          <a
            href="/hacer-cita"
            class="w-full md:w-auto inline-flex items-center justify-center gap-3 bg-gradient-to-r from-pink-600 to-pink-700 text-white px-8 py-4 rounded-full font-semibold text-lg hover:from-pink-700 hover:to-pink-800 transition-all shadow-lg"
          >
            Agenda tu cita
        </a>
        </div>
      </div>
    </div>
  </div>
</section>
