---
import { Phone, Heart, Clock, Users } from "@lucide/astro";
---

<section
  class="bg-gradient-to-br from-pink-600 to-pink-700 text-white py-8 md:py-8"
>
  <div class="max-w-7xl mx-auto p-8">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-8">
        <div
          class="inline-flex items-center gap-2 bg-white/20 backdrop-blur-sm rounded-full px-4 py-2 text-sm"
        >
          <Heart class="w-4 h-4" />
          <span>Tecnología médica avanzada</span>
        </div>

        <div class="space-y-6">
          {/* p grande como titular */}
          <p class="text-left text-4xl md:text-5xl lg:text-6xl font-bold leading-tight text-white">
            Una biopsia de tiroides no tiene por qué ser incómoda
          </p>

          {/* h1 debajo del p grande */}
          <h1 class="text-left text-xl md:text-2xl lg:text-3xl font-semibold text-pink-100 leading-tight">
            Biopsia de tiroides guiada por ultrasonido
          </h1>

          {/* párrafo más pequeño */}
          <p class="text-base md:text-xl text-pink-100 leading-relaxed">
            Utilizamos anestesia local, guía ecográfica y contamos con más de 10
            años de experiencia para que tu estudio sea seguro y con mínima
            molestia.
          </p>
        </div>

        <a
          href="/hacer-cita"
          class="w-full md:w-auto inline-flex items-center gap-3 justify-center md:justify-start bg-white text-pink-600 px-8 py-4 rounded-lg font-semibold hover:bg-gray-50 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1"
          aria-label="Agendar cita"
        >
          <Phone class="w-5 h-5" />
          Agenda tu Cita
      </a>
      </div>

      <div class="relative">
        <div
          class="bg-white/10 backdrop-blur-sm rounded-3xl p-8 overflow-hidden"
        >
          <img
            src="/biopsia-tiroidea-ecodirigida/doctoreshaciendounabiopsiatiroideaecodirigida.webp"
            alt="Doctores haciendo una biopsia tiroidea ecodirigida"
            class="w-full h-80 object-cover rounded-2xl"
          />

          {/* Floating stats cards */}
          <div
            class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm rounded-2xl p-4 shadow-lg"
          >
            <div class="flex items-center gap-2">
              <Clock class="w-5 h-5 text-pink-600" />
              <div>
                <p class="text-sm font-semibold text-gray-900">20-30 min</p>
                <p class="text-xs text-gray-600">Duración</p>
              </div>
            </div>
          </div>

          <div
            class="absolute bottom-4 left-4 bg-white/90 backdrop-blur-sm rounded-2xl p-4 shadow-lg"
          >
            <div class="flex items-center gap-2">
              <Users class="w-5 h-5 text-pink-600" />
              <div>
                <p class="text-sm font-semibold text-gray-900">2 Radiólogos</p>
                <p class="text-xs text-gray-600">Especialistas</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
