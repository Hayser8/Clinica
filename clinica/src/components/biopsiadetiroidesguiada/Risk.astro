---
import { Shield, CheckCircle, AlertTriangle, Users, Award } from "@lucide/astro";
const safetyFeatures = [
  { icon: Shield, text: "Bajo riesgo", description: "Procedimiento mínimamente invasivo" },
  { icon: Users, text: "Especialistas expertos", description: "Más de 10 años de experiencia" },
  { icon: CheckCircle, text: "Guía ecográfica", description: "Precisión en tiempo real" },
]
---
<section
  class="py-16 md:py-24 bg-white"
>
  <div class="max-w-7xl mx-auto p-8">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div
        class="lg:col-span-2 w-full bg-white rounded-3xl p-8 md:p-12 shadow-xl border border-gray-100"
      >
        <div class="flex items-start gap-6 mb-8">
          <div
            class="w-16 h-16 bg-gradient-to-br from-pink-500 to-pink-600 rounded-2xl flex items-center justify-center flex-shrink-0 shadow-lg"
          >
            <Shield class="w-8 h-8 text-white" />
          </div>
          <div class="flex-1">
            <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
              ¿Qué tan riesgoso es una biopsia de tiroides guiada por
              ultrasonido?
            </h2>
          </div>
        </div>

        <div class="space-y-6 mb-8">
          <div
            class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-6 border border-green-100"
          >
            <div class="flex items-center gap-3 mb-3">
              <CheckCircle class="w-6 h-6 text-green-600" />
              <h3 class="font-bold text-green-800">
                Procedimiento de bajo riesgo
              </h3>
            </div>
            <p class="text-lg text-gray-700 leading-relaxed">
              Es un estudio de bajo riesgo. En casos poco frecuentes puede
              presentarse un pequeño hematoma o dolor leve en el sitio de la
              punción.
            </p>
          </div>

          <div
            class="bg-gradient-to-r from-pink-50 to-pink-100 rounded-2xl p-6 border border-pink-100"
          >
            <div class="flex items-center gap-3 mb-3">
              <AlertTriangle class="w-6 h-6 text-pink-600" />
              <h3 class="font-bold text-pink-800">Complicaciones mínimas</h3>
            </div>
            <p class="text-gray-700">
              Con la guía ecográfica y la experiencia de nuestros especialistas,
              las complicaciones son mínimas y manejables.
            </p>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          {
            safetyFeatures.map((feature, index) => (
              <div key={index} class="bg-gray-50 rounded-xl p-4 text-center">
                <feature.icon class="w-8 h-8 text-pink-600 mx-auto mb-2" />
                <h4 class="font-semibold text-gray-800 mb-1">{feature.text}</h4>
                <p class="text-sm text-gray-600">{feature.description}</p>
              </div>
            ))
          }
        </div>

        {/* Botón: centrado en desktop, full en móvil */}
        <div class="flex justify-start md:justify-center">
          <button
            class="w-full md:w-auto inline-flex items-center justify-center gap-3 bg-gradient-to-r from-pink-600 to-pink-700 text-white px-8 py-4 rounded-full font-semibold text-lg hover:from-pink-700 hover:to-pink-800 transition-all shadow-lg"
          >
            <Shield class="w-5 h-5" />
            Agenda tu cita
          </button>
        </div>
      </div>
    </div>
  </div>
</section>
