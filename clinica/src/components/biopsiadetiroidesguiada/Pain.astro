---
import { Heart, Smile, Clock, Shield, ThumbsUp } from "@lucide/astro";
const comfortFeatures = [
  { icon: Shield, text: "Anestesia local", description: "Adormecemos completamente la zona" },
  { icon: Clock,  text: "Procedimiento rápido", description: "Solo toma 15-30 minutos" },
  { icon: ThumbsUp, text: "Mínima molestia", description: "Similar a una inyección común" },
];
---

<section class="py-16 md:py-24 bg-pink-50 overflow-x-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-8 py-8">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div
        class="lg:col-span-2 w-full max-w-full bg-white rounded-3xl p-8 md:p-12 shadow-xl border border-gray-100"
      >
        {/* Header: icono + título (apilado en mobile) */}
        <div class="flex flex-col md:flex-row items-start gap-4 md:gap-6 mb-8">
          <div
            class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-pink-500 to-pink-600 rounded-xl md:rounded-2xl flex items-center justify-center flex-shrink-0 shadow-lg"
          >
            <Heart class="w-7 h-7 md:w-8 md:h-8 text-white" />
          </div>
          {/* min-w-0 evita empujar el layout y generar overflow */}
          <div class="flex-1 min-w-0">
            <h2 class="text-2xl md:text-3xl font-bold text-gray-900">
              ¿Es un procedimiento doloroso?
            </h2>
          </div>
        </div>

        <div class="space-y-6 mb-8">
          <div
            class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-6 border border-green-100"
          >
            <div class="flex items-center gap-3 mb-3">
              <Smile class="w-6 h-6 text-green-600" />
              <h3 class="font-bold text-green-800">Respuesta corta: No</h3>
            </div>
            <p class="text-lg text-gray-700 leading-relaxed">
              La mayoría de los pacientes refieren solo una ligera molestia,
              similar a un pinchazo de inyección.
            </p>
          </div>

          <p class="text-lg text-gray-600">
            La anestesia local adormece la zona y el ultrasonido asegura que el
            procedimiento sea rápido y seguro. Puede quedar una leve
            sensibilidad en el cuello, que suele desaparecer en horas.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
          {
            comfortFeatures.map((feature, index) => (
              <div key={index} class="bg-gray-50 rounded-xl p-4 text-center">
                <feature.icon class="w-8 h-8 text-pink-600 mx-auto mb-2" />
                <h4 class="font-semibold text-gray-800 mb-1">{feature.text}</h4>
                <p class="text-sm text-gray-600">{feature.description}</p>
              </div>
            ))
          }
        </div>

        {/* CTA: full en mobile, centrado en desktop */}
        <div class="flex justify-start md:justify-center">
          <button
            class="w-full md:w-auto inline-flex items-center justify-center gap-3 bg-gradient-to-r from-pink-600 to-pink-700 text-white px-8 py-4 rounded-full font-semibold text-lg hover:from-pink-700 hover:to-pink-800 transition-all shadow-lg"
          >
            Agenda tu cita
          </button>
        </div>
      </div>
    </div>
  </div>
</section>
