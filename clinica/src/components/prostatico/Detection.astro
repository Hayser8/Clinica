---
import { CheckCircle, Phone } from "@lucide/astro";
const detections = [
  "Cambios en tamaño y textura de la glándula",
  "Nódulos o áreas sospechosas que requieren seguimiento clínico",
  "Alteraciones asociadas a síntomas urinarios (chorro débil, vaciado incompleto), con apoyo en la medición de residuo postmiccional",
  "Evaluación de vesículas seminales y de vejiga antes y después de orinar, en el caso de ultrasonido vía suprapúbica",
];
---

<section class="py-20 md:py-24 bg-white">
  <div class="max-w-7xl mx-auto p-8">
    <div class="mx-auto">
      <!-- Título centrado y sin icono -->
      <div class="mb-12 md:mb-16">
        <h2 class="text-center text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 leading-tight">
          ¿Qué puede detectar una ecografía prostática?
        </h2>
      </div>

      <div class="grid gap-6">
        {detections.map((detection, index) => (
          <div
            key={index}
            class="flex items-start gap-6 p-6 lg:p-8 bg-gray-50 rounded-2xl hover:bg-gray-100 transition-colors"
          >
            <div class="bg-pink-600 p-2 rounded-full flex-shrink-0 mt-1">
              <CheckCircle class="w-5 h-5 text-white" />
            </div>
            <p class="mt-1.5 text-base md:text-lg text-gray-700 leading-relaxed">
              {detection}
            </p>
          </div>
        ))}
      </div>

      <!-- CTA -->
      <div class="text-center mt-10">
        <a
          href="/hacer-cita"
          class="inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold bg-gradient-to-r from-pink-600 to-pink-700 text-white hover:from-pink-700 hover:to-pink-800 transition-colors w-full sm:w-auto"
          aria-label="Agenda tu Cita"
        >
          <Phone class="w-5 h-5 mr-2" />
          Agenda tu Cita
        </a>
      </div>
    </div>
  </div>
</section>
