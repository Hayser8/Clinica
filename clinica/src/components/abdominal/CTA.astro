---
import { Phone, MessageCircle } from "@lucide/astro"
import Button from "@/components/ui/Button.astro";
---
<section class="py-20 bg-gradient-to-br from-pink-600 to-pink-700 text-white">
      <div class="max-w-7xl mx-auto px-8 text-center">
        <h2 class="text-4xl font-bold mb-8">Da el primer paso hacia un diagnóstico claro</h2>

        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <Button size="lg" class="bg-white text-pink-600 hover:bg-pink-50">
            <MessageCircle class="w-5 h-5 mr-2" />
            WhatsApp: 4565 0865
          </Button>
          <Button size="lg" variant="outline" class="border-white text-white hover:bg-white/10 bg-transparent">
            <Phone class="w-5 h-5 mr-2" />
            Teléfono: 2448 1110
          </Button>
        </div>
      </div>
    </section>