---
import { AlertCircle, CheckCircle } from "@lucide/astro";

  const conditions = [
    "Dolor o distensión abdominal",
    "Función anormal del hígado",
    "Órgano Abdominal agrandando",
    "Cálculos renales o biliares",
    "Agrandamiento prostático",
    "Evaluación de útero y ovarios",
  ]
---
<section class="py-20 bg-gray-50">
  <div class="max-w-7xl mx-auto px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-6">
        ¿Cuándo realizar un ultrasonido abdominal?
      </h2>
      <p class="text-xl text-gray-600 max-w-4xl mx-auto leading-relaxed">
        ¿Has experimentado molestias después de las comidas, distensión sin
        sentido o sensibilidad al tacto? Un ultrasonido puede orientar y
        acelerar tu diagnóstico. Además, son útiles si quieres descartar o si
        tienes patologías crónicas en órganos clave como hígado, riñones y
        páncreas, realízalo para un monitoreo continuo y sin exposición a
        radiación.
      </p>
    </div>

    <div class="bg-white rounded-2xl p-8 shadow-lg">
      <div class="flex items-center gap-3 mb-8">
        <div class="inline-flex items-center justify-center w-10 h-10 bg-pink-600 rounded-lg">
          <AlertCircle class="w-6 h-6 text-white" />
        </div>
        <h3 class="text-2xl font-bold text-gray-900">
          El ultrasonido se utiliza para ayudar a diagnosticas distintas
          patologías como:
        </h3>
      </div>

      <div class="grid md:grid-cols-2 gap-4">
        {
          conditions.map((condition, index) => (
            <div key={index} class="flex items-center gap-3">
              <CheckCircle class="w-5 h-5 text-pink-600 flex-shrink-0" />
              <span class="text-gray-700">{condition}</span>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
